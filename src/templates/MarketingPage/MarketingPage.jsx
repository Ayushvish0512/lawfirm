export function MarketingPage({
  hero,
  introduction,
  features,
  benefits,
  testimonial,
  cta,
}) {
  return (
    <>
      {hero}
      {introduction}
      {features}
      {benefits}
      {testimonial}
      {cta}
    </>
  );
}

export default MarketingPage;
