# tahoma  Fonts Placeholder
Place the following `.woff2` files here when available:
- `tahoma -Regular.woff2`
- `Tahoma -Medium.woff2`
- `Tahoma-SemiBold.woff2`
- `Tahoma-Bold.woff2`

Once uploaded, uncomment the `@font-face` blocks in `src/styles/fonts.css` and update `--font-primary` in `src/styles/tokens.css` to `'Tahoma'`.
