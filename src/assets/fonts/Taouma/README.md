# Taouma Fonts Placeholder
Place the following `.woff2` files here when available:
- `Taouma-Regular.woff2`
- `Taouma-Medium.woff2`
- `Taouma-SemiBold.woff2`
- `Taouma-Bold.woff2`

Once uploaded, uncomment the `@font-face` blocks in `src/styles/fonts.css` and update `--font-primary` in `src/styles/tokens.css` to `'Taouma'`.
